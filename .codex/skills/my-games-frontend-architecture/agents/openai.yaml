interface:
  display_name: "My Games Frontend Architecture"
  short_description: "Frontend architecture and cache rules"
  default_prompt: "Use $my-games-frontend-architecture to change the My Games frontend while preserving TanStack Query cache correctness."
policy:
  allow_implicit_invocation: true
